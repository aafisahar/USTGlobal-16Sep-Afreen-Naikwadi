//Applying for loop to array object created using new keyword
   //1.array
   for(var i;i<fruits.length;i++)
   {
       console.log(fruits[i]);
   } 
   //2.array
   for(var i;i<mobile.length;i++)
   {
       console.log(mobile[i]);
   } 
    //3.array
    for(var i;i<car.length;i++)
   {
       console.log(car[i]);
   } 
    //4.array
    for(var i;i<country.length;i++)
   {
       console.log(country[i]);
   } 
    //2.array
    for(var i;i<city.length;i++)
   {
       console.log(city[i]);
   } 
