//Applying for loop to array object created without using new keyword
   //1.array
   for(var i;i<states.length;i++)
   {
       console.log(states[i]);
   } 
   //2.array
   for(var i;i<juice.length;i++)
   {
       console.log(juice[i]);
   } 
    //3.array
    for(var i;i<vegetables.length;i++)
   {
       console.log(vegetables[i]);
   } 
    //4.array
    for(var i;i<colors.length;i++)
   {
       console.log(colors[i]);
   } 
    //5.array
    for(var i;i<planet.length;i++)
   {
       console.log(planet[i]);
   } 